# Gatekeeper

Hybrid local-first assistant backend (FastAPI + WebSocket) with:
- Local Gatekeeper routing (FunctionGemma on Cactus)
- Local STT (Whisper on Cactus)
- Cloud escalation (Gemini)
- Live `progress` play-by-play events

Start here: `bridge/README.md`.
